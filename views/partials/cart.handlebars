<div id="cart" class="btn-group btn-block">
  <button type="button" data-toggle="dropdown" data-loading-text="Loading..." class="btn btn-inverse btn-block btn-lg dropdown-toggle"><i class="fa fa-shopping-cart"></i> <span id="cart-total">You have {{cart.count}} item(s)</span></button>
  <ul class="dropdown-menu pull-right">
   
    {{#if cart.items}}
    <li>
      <table class="table table-striped">
        
        {{#each cart.items}}
        <tr>
          <td class="text-center">{{#if this.thumb}}
            <a href="product/{{this.product_id}}"><img src="{{this.thumb}}" alt="{{this.product.name}}" title="{{this.product.name}}" class="img-thumbnail" /></a>
            {{/if}}</td>
          <td class="text-left"><a href="/product/{{this.product.product_id}}">{{this.product.name}}</a>
            
            {{#if this.product.options}}
            
            {{#each this.product.options}}
            <br />
            - <small>{{this.name}} {{this.value}}</small>
            
            {{/each}}
            {{/if}}
           
          <td class="text-right">x {{this.quantity}}</td>
          <td class="text-right">{{this.product.total}}</td>
          <td class="text-center"><button type="button" onclick="cart.remove('{{this.itemId}}');" title="delete" class="btn btn-danger btn-xs"><i class="fa fa-times"></i></button></td>
        </tr>
        {{/each}}
        
      </table>
    </li>
    <li>
      <div>
        <table class="table table-bordered">
          
          {{#if cart.total}}
          <tr>
            <td class="text-right"><strong>Total:</strong></td>
            <td class="text-right">{{cart.total}}</td>
          </tr>
          {{/if}}
          
        </table>
        <p class="text-right"><a href="/cart"><strong><i class="fa fa-shopping-cart"></i> Check cart</strong></a>&nbsp;&nbsp;&nbsp;<a href="/checkout"><strong><i class="fa fa-share"></i> Checkout</strong></a></p>
      </div>
    </li>
    {{/if}}
    {{#unless cart.items}}
    <li>
      <p class="text-center">Your cart is empty</p>
    </li>
    {{/unless}}
  </ul>
  
</div>
